import { Inter, UnifrakturCook } from "@next/font/google";

export const titleFont = UnifrakturCook({
  weight: ["700"],
  subsets: ["latin"],
});

export const bodyFont = Inter({
  weight: ["700"],
  subsets: ["latin"],
});
